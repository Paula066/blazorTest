{
  "buildCommand": "dotnet publish -c Release -o output",
  "outputDirectory": "output/wwwroot",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
